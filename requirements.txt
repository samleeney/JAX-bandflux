# Core runtime deps (match setup.py)
numpy>=1.24.0
jax>=0.4.20
sncosmo>=2.9.0
pyyaml>=6.0.0
matplotlib
tqdm
anesthetic
requests
distrax
pytest>=7.0.0

# Nested sampling (Handley Lab fork)
# See: https://handley-lab.co.uk/nested-sampling-book/intro.html
git+https://github.com/handley-lab/blackjax.git

# For GPU/CUDA, install matching jax[cuda*] wheels separately per JAX docs.
